/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  domains: [
    "pravinnayak.me",
  ],
  images: {
    formats: ['image/webp'],
  },
}

module.exports = nextConfig
